* Changes to linear and angular speed persist over time, instead of being reset every cycle
* Gathering all published topics under a common base topic (default is "spur_bridge")
* Support for topic remapping (e.g. "/spur_bridge/cmd_vel" -> "/yamabico/cmd_vel")
* Parameterized spur initialization parameters
* Log messages use rosconsole